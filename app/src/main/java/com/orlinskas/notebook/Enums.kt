package com.orlinskas.notebook

class Enums {
    enum class DownloadStatus {
        LOADING,
        READY
    }

    enum class ConnectionStatus {
        CONNECTION_DONE,
        CONNECTION_FAIL
    }
}
